{
  "schema": "corpus-adequacy.manifest.v0",
  "tool_pin": {
    "_why": "Pinned to corpus-adequacy 13048989, whose common report shape owns deterministic bytes, runner, manifest identity, control status, diagnostic state, and exact tool identity. This row must be re-measured under that contract rather than upgraded mechanically.",
    "commit": "13048989c84ab6b4e0281f9514ea45fb79a2d8b4",
    "tool": "https://github.com/corpus-adequacy/corpus-adequacy"
  },
  "runner": "batch",
  "outcome_parse": "test-names",
  "accepted_exit_codes": [
    0,
    101
  ],
  "_about": "Mutation adequacy for the RFC 8785 (JCS) conformance corpus. The scored denominator is exactly two author-declared rules in the pinned serde_jcs dependency: RFC 8785 UTF-16 object-key ordering and ECMAScript-compatible number serialization. It is not RFC certification, corpus coverage, or full implementation adequacy. The existing wrapper swap remains a separate control proving the runner, mutation and scorer are connected. ryu-js is the pinned transitive number-formatting oracle, not a mutable subject.",
  "repo_root": "../..",
  "implementation_sources": [
    "../../crates/assay-canonical/src/jcs.rs",
    "../../third_party/serde_jcs-0.2.0/src/lib.rs"
  ],
  "harness_sources": [
    "../../scripts/ci/adequacy_serde_jcs.py",
    "../../third_party/serde_jcs-0.2.0.crate",
    "../../third_party/serde_jcs-0.2.0.provenance.json"
  ],
  "build": [
    "python3",
    "scripts/ci/adequacy_serde_jcs.py",
    "build"
  ],
  "entrypoint_command": [
    "python3",
    "scripts/ci/adequacy_serde_jcs.py",
    "test"
  ],
  "vectors": "../../crates/assay-canonical/tests/vectors/rfc8785.json",
  "id_key": "vector_id",
  "default_group": "jcs_canonicalization",
  "vector_timeout": 900,
  "mutants": {
    "jcs_canonicalization": [
      {
        "label": "CONTROL the corpus still bites (serde_json instead of serde_jcs)",
        "control": true,
        "anchor": "serde_jcs::to_vec(value)",
        "replacement": "serde_json::to_vec(value)"
      },
      {
        "label": "RFC 8785 section 3.2.3 UTF-16 object-key ordering",
        "anchor": "self.tag.cmp(&other.tag)",
        "replacement": "self.key.cmp(&other.key)"
      },
      {
        "label": "RFC 8785 section 3.2.2.3 and Appendix B ECMAScript-compatible number serialization",
        "anchor": "buffer.format_finite(value)",
        "replacement": "({ let _ = &mut buffer; value.to_string() })"
      }
    ]
  },
  "equivalent": {}
}

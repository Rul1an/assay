# privileged-mcp-action/v1 conformance corpus

Focused vectors for [`docs/profiles/privileged-mcp-action/v1.md`](../../docs/profiles/privileged-mcp-action/v1.md).
The historical v0 corpus is untouched.

Selection is explicit: `assay evidence verify-privileged-mcp-action --profile-version v1`.
Default `v0` must not autodetect these bundles.

`gen_vectors.py` is byte-deterministic (Python 3 stdlib). Re-run it to refresh
`vectors/` and `MANIFEST.json`.

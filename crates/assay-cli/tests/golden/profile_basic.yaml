# Generated by `assay sandbox --profile`
api_version: 1
extends:
  - pack:deny-all
  - pack:mcp-server-minimal
fs:
  allow:
    - "./data/input.txt"
  deny:
net:
  allow: []
  deny:
    - "*:*"
env:
  allow:
    - "FOO_FEATURE"
processes:
  allow:
    - "/usr/bin/cat"
meta:
  counters:
    "sandbox.degrade_to_audit": 1
    "sandbox.env_strict_used": 1
  notes:
    - "degraded: landlock policy conflict"

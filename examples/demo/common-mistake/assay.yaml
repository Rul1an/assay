version: 1
suite: "demo-common-mistake"
model: "dummy"



# Minimal stub so validate can load config; no traces required for policy validation.
tests:
  - id: "placeholder-smoke"
    input: "ignore"
    expected:
      must_contain: []

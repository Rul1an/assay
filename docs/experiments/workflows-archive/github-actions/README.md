# Archived experiment workflows

These GitHub Actions workflows were used as manually dispatched experiment
harnesses for retained lab findings. They are kept here as reproducibility
context, but they are intentionally outside `.github/workflows/` so they do not
appear as active repository workflows or PR/status surfaces.

To re-run one, review it as historical experiment code first and copy it back to
`.github/workflows/` in a dedicated PR.

# Cross-session decay fixtures

This variant is primarily controlled by:
- `DECAY_RUNS` (run-count decay window)
- `CROSS_SESSION_PHASE` (`read_only` | `sink_only` | `legit_control`)

Step2 does not require new payload fixtures beyond existing experiment inputs.
